import { createGlobalStyle } from 'styled-components';

const GlobalColors = createGlobalStyle`
    :root{
      --main: #1b262c;
      --main-secondary: #0f4c75;
      --main-thirth: #3282b8;
      --main-forth: #bbe1fa;
    };
`;

export default GlobalColors;